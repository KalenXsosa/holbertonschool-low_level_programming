#include <stdio.h>
/**
* main - printing alphabet in lowercase
* Return: always 0
*/

int main(void)

{
char power;

for (power = 'a'; power <= 'z'; power++)
putchar(power);
putchar('\n');

return (0);
}
