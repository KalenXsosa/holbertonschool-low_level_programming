#include <stdio.h>
/**
* main - printing alphabet in lowercase and uppercasei
*
* Return: always 0
*/
int main(void)
{
char x;
char y;
for (x = 'A'; x <= 'Z'; x++)
putchar(x);
for (y = 'a'; x <= 'z'; x++)
putchar(y);
return (0);
}
