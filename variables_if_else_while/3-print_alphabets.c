#include <stdio.h>
/**
* main - printing alphabet in lowercase and uppercasei
*
* Return: always 0
*/
int main(void)
{
char x;
char y;
for (x = 'a'; x <= 'z'; x++)
putchar(x);
for (y = 'A'; x <= 'Z'; y++)
putchar(y);
return (0);
}
